# State

Canonical machine-readable project state:

- `CURRENT_PROJECT_STATE.json`

Latest synchronization marker:

- `STATE_SYNC_4_35.md`

Historical phase closure observations remain tied to their closure checkpoints; later activation-preflight evidence is additive and does not rewrite those historical states.
